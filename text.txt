 <LinearGradient
        colors={["#FFF6F2", "#FDE2DD", "#ffc0cb"]} 
        start={{ x: 0, y: 0 }}
        end={{ x: 1, y: 1 }}
        className="absolute top-0 left-0 right-0 h-[45%]"
      />

      <SafeAreaView className="flex-1">
        {/* 2. HEADER CONTENT: Title & Counter */}
        {/* This takes up the top space so the sheet sits below it */}
        <View className="h-[35%] items-center justify-center space-y-4">
            {/* Missing Title Added */}
            <Text className="text-3xl font-bold text-gray-900 tracking-wider">
              TaskFlow
            </Text>
            
            {/* Counter Component */}
            <View className="mt-2">
               <TaskCounter count={tasks.length} />
            </View>
        </View>

        {/* 3. WHITE SHEET: The List Container */}
        {/* rounded-t-[40px] gives that curved sheet look */}
        <View className="flex-1 bg-white rounded-t-[40px] px-5 pt-4 shadow-2xl overflow-hidden">
            
            {/* Visual Drag Handle */}
            <View className="items-center mb-6 pt-2">
              <View className="w-12 h-1.5 bg-gray-200 rounded-full" />
            </View>

            {/* Filters Row */}
            <View className="mb-6">
              <TaskFilters />
            </View>

            {/* Scrollable Task List */}
            <ScrollView 
              className="flex-1" 
              showsVerticalScrollIndicator={false}
              contentContainerStyle={{ paddingBottom: 100 }} // Extra space for FAB
            >
              <View className="space-y-4">
                {tasks.map((task) => (
                  <Task key={task.id} task={task} />
                ))}
              </View>
            </ScrollView>
        </View>
      </SafeAreaView>

      {/* 4. FLOATING ACTION BUTTON (FAB) */}
      {/* Absolute positioned to sit on top of everything */}
      <TouchableOpacity 
        className="absolute bottom-10 right-6 w-16 h-16 bg-[#ff4d6d] rounded-full items-center justify-center shadow-lg elevation-5"
        activeOpacity={0.8}
      >
        <Text className="text-white text-4xl font-light mb-1">+</Text>
      </TouchableOpacity>